1,492 characters — well within the 4,000 target. Script is working correctly.

`.lathe/snapshot.sh` is written and executable. It covers:

- **Build** — `cargo build`, single-line OK or error list on failure
- **Tests** — aggregated pass/fail/ignored counts across all test binaries (currently: Pass: 2121 | Fail: 0)
- **Clippy** — `cargo clippy -- -D warnings`, OK or error count + first 10 errors
- **Unsafe audit** — grep for `unsafe` blocks, surfaces violations immediately
- **CI** — lists `.github/workflows/` files
- **Git status + recent commits**

Output is ~1,500 chars at baseline (well under the 4,000 target), with failure paths expanding inline only when something breaks. Uses `gtimeout` fallback for macOS.
