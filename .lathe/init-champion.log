All four files are written. Here's what was produced:

---

**`.lathe/agents/champion.md`** — The runtime playbook. Three stakeholders grounded in what the code actually shows:

- **Lead Researcher** — drives the frontier, emotional signal is *momentum*
- **Spec Researcher** — navigates `refs/fls-ambiguities.md`, emotional signal is *confidence and authority*
- **Compiler Contributor** — extends the pipeline, emotional signal is *clarity*

The ranking model: CI floor first (red build → no journey, fix the build), then lived experience above the floor. No layer ladder — the `How to Rank` section names the two sources explicitly and flags the anti-pattern by name.

**`.lathe/skills/journeys.md`** — Concrete first-encounter journeys per stakeholder, with harder journeys for when the easy path completes. The Lead Researcher's journey pushes until the compile actually fails on something real; the Spec Researcher's harder journey requires finding *all* findings for a section without scanning the full file.

**`.lathe/skills/testing.md`** — How galvanic tests: unit (inline), smoke (CLI-level binary), FLS fixtures (parse acceptance), assembly inspection (the critical invariant for const-folding detection), e2e (QEMU execution), benchmarks. Includes the full CI job table and the "no compile-time results in e2e tests" gotcha.

**`.lathe/skills/architecture.md`** — Pipeline, two-tier lowering, FLS traceability convention, cache-line discipline, domain boundaries (FLS vs ARM64 ISA vs platform ABI), partial-output semantics.

**`.lathe/alignment-summary.md`** — For you. Key items flagged: `ambition.md` is missing (champion falls back to journey-only maturation until you write it), `brand.md` is missing, default branch protection was not verified, potential prompt injection surface noted.
