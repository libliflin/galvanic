I need you to run one command to make `falsify.sh` executable — I don't have permission to run chmod in this session:

```
chmod +x /Users/williamlaffin/code/galvanic/.lathe/falsify.sh
```

Then verify it runs to completion (the last line should print `=== Summary ===`):

```
cd /Users/williamlaffin/code/galvanic && bash .lathe/falsify.sh
```

---

Here's what was written:

**`.lathe/agent.md`** — The runtime agent. Three real stakeholders: the spec investigator (FLS citation fidelity), the cache-line codegen researcher (enforced layout budgets), and the FLS contributor (clean patterns + passing tests). Each has a named load-bearing claim. Tensions are documented honestly — fidelity beats coverage, forever.

**`.lathe/claims.md`** — Seven claims, each named and stakeholder-tagged: Token==8 bytes, Span==8 bytes, build succeeds, tests pass, no unsafe in library, milestone_1 full pipeline, FLS citations present in every source module.

**`.lathe/falsify.sh`** — Runs all seven claims in order, prints per-claim ok/FAIL, ends with `=== Summary === passed: N failed: M`. Uses `|| true` throughout any grep-in-pipeline to avoid `set -uo pipefail` killing it on a legitimate no-match.

**`.lathe/skills/architecture.md`** — Pipeline diagram, FLS traceability convention (inline citations, `AMBIGUOUS:` / `NOTE:` tags), cache-line enforcement scope, IR growth pattern, test layer summary.

**`.lathe/skills/testing.md`** — All four test layers with copy-pasteable patterns, fixture naming convention, adversarial fixture guidance, and the critical "don't test only literals" rule.

**`.lathe/alignment-summary.md`** — Plain-English brief for you. Includes: the one weak claim (C2/Span test may not exist), the macOS/qemu local-vs-CI split, the 1.1MB e2e file I couldn't read, and the repo security observation (public repo, `pull_request` trigger is safe, branch protection unknown).
